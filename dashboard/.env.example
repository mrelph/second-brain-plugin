# Absolute path to your second-brain vault (the folder containing .second-brain.json).
# If unset, the dashboard falls back to the bundled sample-vault/ and shows a banner.
#
# VAULT_PATH=/Users/you/notes/work-brain
VAULT_PATH=
