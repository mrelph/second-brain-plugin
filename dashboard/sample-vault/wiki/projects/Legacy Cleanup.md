# Legacy Cleanup

Tidy up old repos and decommission the v1 services. No frontmatter on this page
on purpose — the dashboard should still classify it as a **project** from the
`projects/` folder and show its status as unknown.

Related to [[Data Migration]].
